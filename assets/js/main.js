/**
 * Arquivo: main.js
 * Descrição: Entry point principal da aplicação. 
 * Responsável por inicializar configurações e preparar o ambiente.
 */

document.addEventListener('DOMContentLoaded', () => {
    initApp();
});

function initApp() {
    console.log('📦 Aplicação iniciada com sucesso!');
    // Futuro: Verificar sessão no LocalStorage
    // Futuro: Inicializar componentes globais (ex: Notificações)
}